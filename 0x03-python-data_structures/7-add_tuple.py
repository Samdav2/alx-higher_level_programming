#!/bin/usr/python3
def add_tuple(tuple_a=(), tuple_b=()):
    add[]
    if len(tuple_b) < 1:
        add[0] = tuple_a[0] + tuple_b[0]
        add[1] = tuple_a[1] + 0
    else:
        add[0] = tuple_a[0] + tuple_b[0]
        add[1] = tuple_a[1] + tuple_b[1]
    return add
