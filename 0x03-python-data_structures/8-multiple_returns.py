#!/usr/bin/python3
def multiple_returns(sentence):
    a = len(sentence)
    word = sentence
    if word == None:
        word[0] = None
        return(a, word[0])
    else:
        return(a, word[0])
