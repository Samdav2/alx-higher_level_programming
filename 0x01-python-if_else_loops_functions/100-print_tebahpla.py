#!/usr/bin/python3
for i in range(ord('a'), ord('z') + 1):
    k = 1
    k += 1
    if k % 2 == 0:
        i -= 32
        print("{}" .format(chr(i), end="")
    else:
        print("{}" .format(chr(i), end="")
