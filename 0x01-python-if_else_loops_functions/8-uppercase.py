#!/usr/bin/python3
def uppercase(str):
    for i in enumerate(str):
        if i not in (i) == " ":
            i = ord(i)
            i -= 32
            i = chr(i)
            print("{i}" .format(i))
        else:
            print("{i}" .format((i)))
